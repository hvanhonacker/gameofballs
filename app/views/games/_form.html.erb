<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :player_1_name %>
    <%= form.text_field :player_1_name %>
  </div>

  <div class="field">
    <%= form.label :player_2_name %>
    <%= form.text_field :player_2_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
